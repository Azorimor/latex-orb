description: >
  Compile a tex project into pdf. The result is available as artifact.

usage:
  version: 2.1
  orbs:
    latex-orb: azorimor/latex-orb@x.x.x
  workflows:
    build:
      jobs:
        - latex-orb/build:
            file: /path/to/main.tex
